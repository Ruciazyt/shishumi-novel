# 史书墨 - 开发任务计划

> 本文档为「史书墨」App 的完整开发任务计划，按 Phase 划分，包含任务清单、优先级、预计工时和验收标准。

---

## Phase 1：项目初始化

### 目标
完成开发环境搭建、项目脚手架完善、基础架构搭建，确保后续功能开发可顺利进行。

---

### 任务清单

#### 1.1 开发环境检查与配置
- **任务名称**：开发环境检查与配置
- **详细描述**：确认 Node.js、npm、Expo CLI、Android SDK 版本符合要求；配置国内镜像源（淘宝 npm）加速依赖安装；配置 ESLint + Prettier 统一代码风格。
- **优先级**：P0
- **预计工时**：1小时
- **验收标准**：
  - `npx expo --version` 可正常执行
  - `npm install` 可在 10 分钟内完成依赖安装
  - ESLint + Prettier 配置已添加，commit 前自动 lint 通过

---

#### 1.2 项目目录结构规范化
- **任务名称**：项目目录结构规范化
- **详细描述**：基于 `src/` 现有结构（components/constants/context/data/screens services types），补充缺失目录（utils/、hooks/、assets/poems/、assets/era-data/），编写各目录的 `index.ts` 统一导出。
- **优先级**：P0
- **预计工时**：2小时
- **验收标准**：
  - `src/` 下所有子目录有明确职责分工
  - 新增 `src/utils/`（通用工具函数）、`src/hooks/`（自定义 Hooks）、`src/assets/poems/`（诗词数据）、`src/assets/era-data/`（时代背景数据）
  - 各目录 index.ts 导出正常，无循环引用

---

#### 1.3 类型定义补充与完善
- **任务名称**：TypeScript 类型定义补充
- **详细描述**：对照 SPEC.md 第 6 节数据结构和 API 设计，补充完整 TypeScript 类型定义（Project、Chapter、Era、PoomRecommendation、ApiResponse 等），放置于 `src/types/`。
- **优先级**：P0
- **预计工时**：2小时
- **验收标准**：
  - `src/types/` 下包含所有业务类型定义
  - `tsc --noEmit` 无类型错误
  - 所有 API 响应和请求数据结构有对应类型

---

#### 1.4 状态管理架构搭建
- **任务名称**：React Context 状态管理架构搭建
- **详细描述**：搭建全局状态管理，参考 SPEC.md 的 React Context + useReducer 方案。创建 ProjectContext（管理项目列表）、EditorContext（管理当前写作状态）、SettingsContext（管理 API Key、主题设置）。
- **优先级**：P0
- **预计工时**：3小时
- **验收标准**：
  - 三个 Context 均已创建且可正常使用
  - Reducer 覆盖所有状态变更场景（增删改）
  - 状态持久化至 AsyncStorage，重启后可恢复

---

#### 1.5 通义千问 API 服务封装
- **任务名称**：通义千问 API 服务封装
- **详细描述**：在 `src/services/` 下创建 `qwenApi.ts`，封装与通义千问的通信。实现：API Key 配置读取、请求超时处理（30s）、错误重试（最多 3 次）、四种 Prompt 模板调用（润色/历史还原/诗词推荐/佛道经典）。
- **优先级**：P0
- **预计工时**：4小时
- **验收标准**：
  - `qwenApi.polishText()`、`qwenApi.restoreHistoricalDetails()`、`qwenApi.recommendPoems()`、`qwenApi.recommendClassics()` 均可用
  - API Key 不硬编码，从 SettingsContext 或环境变量读取
  - 网络异常时给出友好提示而非崩溃

---

#### 1.6 时代背景数据准备
- **任务名称**：时代背景数据初始化
- **详细描述**：在 `src/assets/era-data/` 下创建各朝代（唐/宋/元/明/清）的 JSON 数据文件，包含：语言特点、服饰特征、建筑风格、礼仪制度、代表器物、常见地名。确保 AI 调用时可通过朝代 ID 快速注入背景信息。
- **优先级**：P1
- **预计工时**：3小时
- **验收标准**：
  - 已覆盖唐、宋、元、明、清五个朝代的基础数据
  - 每个朝代数据包含至少 6 个维度的描述信息
  - 数据可通过 `getEraData(dynastyId)` 工具函数获取

---

#### 1.7 诗词与佛道经典数据准备
- **任务名称**：诗词与经典数据初始化
- **详细描述**：在 `src/assets/poems/` 下创建分类诗词数据（唐诗、宋词、诗经）、佛道经典名句数据。每条数据包含：标题、作者、全文、适用场景标签、推荐理由。
- **优先级**：P1
- **预计工时**：3小时
- **验收标准**：
  - 至少收录 50 首常用古诗词（按场景分类）
  - 收录 20 条佛道经典名句
  - 每条数据有 `sceneTags` 便于 AI 辅助检索或关键词匹配

---

### Phase 1 验收标准汇总
- [ ] 开发环境可正常启动 Expo 开发服务器
- [ ] TypeScript 无类型错误
- [ ] 三个 Context 状态管理正常工作
- [ ] 通义千问 API 调用成功返回结果
- [ ] 时代背景数据可正常读取
- [ ] 所有代码通过 ESLint 检查

---

## Phase 2：核心功能开发

### 目标
完成四大核心模块（项目管理、AI辅助写作、资料查阅、诗词推荐）的完整实现。

---

### 模块 2.1：AI辅助写作模块

#### 2.1.1 富文本编辑器基础实现
- **任务名称**：富文本编辑器基础实现
- **详细描述**：在 `src/screens/EditorScreen.tsx` 实现基于 TextInput 的章节编辑器，支持：大文本输入、自动保存（每 30 秒）、字数统计、撤销/重做（记录最近 20 步操作历史）。
- **优先级**：P0
- **预计工时**：6小时
- **验收标准**：
  - 可输入并编辑万字以上章节文本，输入流畅无卡顿
  - 自动保存触发后有提示（Toast）
  - 字数实时显示
  - 撤销/重做操作正常

---

#### 2.1.2 AI润色功能集成
- **任务名称**：AI润色功能集成
- **详细描述**：在编辑器底部 AI 工具栏添加「润色」按钮。用户选中文本后点击润色，调用 `qwenApi.polishText()`，在原地替换润色结果。展示加载状态，支持取消操作。
- **优先级**：P0
- **预计工时**：4小时
- **验收标准**：
  - 选中段落后点击润色，替换为 AI 润色结果
  - 润色过程中显示 loading 状态
  - 润色结果可一键撤销
  - API 超时时提示用户并保留原文

---

#### 2.1.3 历史细节还原功能
- **任务名称**：AI历史细节还原功能
- **详细描述**：在 AI 工具栏添加「还原历史」按钮。根据当前项目的朝代背景，调用 `qwenApi.restoreHistoricalDetails()`，自动识别并修改文本中的现代表述或不准确历史细节。
- **优先级**：P0
- **预计工时**：4小时
- **验收标准**：
  - 可识别服饰、建筑、礼仪、器物等常见历史细节错误
  - 还原前展示 AI 修改意见（diff 视图或原文/新文对比）
  - 用户确认后替换文本
  - 支持撤销

---

#### 2.1.4 AI工具栏 UI
- **任务名称**：AI工具栏 UI 设计与实现
- **详细描述**：设计并实现编辑器底部的 AI 工具栏，包含四个功能按钮（润色/还原历史/查资料/推荐诗词），工具栏在输入时自动隐藏，选中文本后自动出现。支持展开详情面板展示 AI 返回内容。
- **优先级**：P1
- **预计工时**：3小时
- **验收标准**：
  - 工具栏设计符合 SPEC.md 中国风配色
  - 选中文字后工具栏动画出现
  - 各功能按钮点击事件正确触发对应 AI 操作
  - 详情面板展示 AI 返回的完整内容

---

### 模块 2.2：资料查阅模块

#### 2.2.1 资料搜索页面
- **任务名称**：资料搜索页面实现
- **详细描述**：在 `src/screens/ReferenceScreen.tsx` 实现资料查阅页面。用户输入关键词（如"唐代科举"），调用通义千问 API 获取该主题的历史资料摘要，同时可结合本地时代背景数据提供更精准的结果。
- **优先级**：P1
- **预计工时**：4小时
- **验收标准**：
  - 搜索结果以可读性良好的卡片形式展示
  - 支持查看搜索历史记录
  - 无结果时给出友好提示

---

#### 2.2.2 时代背景快速查阅
- **任务名称**：时代背景快速查阅功能
- **详细描述**：在项目管理页和写作页提供朝代快速切换入口，点击后展示该朝代的时代特征概览（服饰/建筑/礼仪/语言），无需联网即可查看本地数据。
- **优先级**：P1
- **预计工时**：3小时
- **验收标准**：
  - 各朝代时代背景概览展示完整
  - 切换朝代后，当前项目的 AI 写作风格自动同步更新
  - 本地数据加载无网络依赖

---

### 模块 2.3：诗词推荐模块

#### 2.3.1 诗词推荐功能
- **任务名称**：场景匹配诗词推荐功能
- **详细描述**：在 AI 工具栏添加「推荐诗词」按钮。根据用户选中的场景文本或当前章节内容，调用 `qwenApi.recommendPoems()`，推荐 1-2 首适合的古诗词，显示诗词名称、作者、全文、推荐理由。
- **优先级**：P1
- **预计工时**：4小时
- **验收标准**：
  - 推荐的诗词与场景匹配度高
  - 诗词卡片展示完整信息
  - 支持一键将诗词插入当前光标位置

---

#### 2.3.2 佛道经典推荐
- **任务名称**：佛道经典引用推荐功能
- **详细描述**：实现「佛道经典」功能按钮，调用 `qwenApi.recommendClassics()`，推荐适合场景的佛教或道家经典语句，显示出处和现代含义解读。
- **优先级**：P2
- **预计工时**：3小时
- **验收标准**：
  - 经典推荐结果包含原文、出处、含义解读
  - 同样支持一键插入文本
  - 推荐内容与佛教/道家场景相关

---

#### 2.3.3 诗词收藏与管理
- **任务名称**：诗词收藏与管理
- **详细描述**：允许用户收藏推荐过的诗词，在设置页提供「我的收藏」入口，支持按朝代/场景标签筛选，收藏数据持久化至 AsyncStorage。
- **优先级**：P2
- **预计工时**：2小时
- **验收标准**：
  - 可收藏/取消收藏诗词
  - 收藏列表按标签筛选正常
  - 重启后收藏数据保留

---

### 模块 2.4：项目管理模块

#### 2.4.1 项目列表页
- **任务名称**：项目列表页面实现
- **详细描述**：在 `src/screens/ProjectListScreen.tsx` 实现首页项目列表。展示所有小说项目（书名、朝代、简介、章节数、最近更新时间），支持按名称或更新时间排序，支持下拉刷新。
- **优先级**：P0
- **预计工时**：3小时
- **验收标准**：
  - 列表展示项目封面信息
  - 排序和筛选功能正常
  - 下拉刷新正常

---

#### 2.4.2 项目创建/编辑
- **任务名称**：项目创建与编辑功能
- **详细描述**：在 `src/screens/ProjectFormScreen.tsx` 实现项目的创建和编辑表单，包含：书名（必填）、朝代选择（下拉菜单）、简介（多行文本）。验证必填项，保存后自动跳转至项目详情页。
- **优先级**：P0
- **预计工时**：3小时
- **验收标准**：
  - 表单验证正常（书名必填）
  - 创建后自动生成项目 ID 并保存
  - 编辑后数据正确更新

---

#### 2.4.3 项目详情页
- **任务名称**：项目详情页实现
- **详细描述**：在 `src/screens/ProjectDetailScreen.tsx` 实现项目详情页，展示项目信息（书名、朝代、简介），下方列出所有章节，支持跳转到写作页。
- **优先级**：P0
- **预计工时**：2小时
- **验收标准**：
  - 展示项目基本信息
  - 章节列表完整
  - 可编辑项目信息、删除项目

---

#### 2.4.4 章节管理
- **任务名称**：章节 CRUD 功能实现
- **详细描述**：实现章节的创建、编辑、重命名、删除功能。在项目详情页提供新增章节按钮，章节支持拖拽排序，长按可删除。
- **优先级**：P0
- **预计工时**：4小时
- **验收标准**：
  - 章节增删改查功能完整
  - 删除时有二次确认
  - 拖拽排序后顺序正确保存

---

### Phase 2 验收标准汇总
- [ ] 项目列表、创建、编辑、删除功能正常
- [ ] 章节增删改查、拖拽排序正常
- [ ] 富文本编辑器支持万字以上文本，输入流畅
- [ ] AI润色功能正常返回结果
- [ ] AI历史细节还原功能正常，支持 diff 对比
- [ ] 资料搜索返回可读的 AI 摘要结果
- [ ] 诗词推荐结果可一键插入文本
- [ ] 佛道经典推荐功能正常
- [ ] AI工具栏 UI 符合中国风设计规范

---

## Phase 3：测试与发布

### 目标
完成全面测试，修复已知问题，构建生产 APK，准备 Release 发布包。

---

### 3.1 单元测试与集成测试
- **任务名称**：单元测试与集成测试
- **详细描述**：为关键模块编写 Jest 单元测试，覆盖：API 服务封装（Mock 响应）、Context Reducer（状态变更）、工具函数（getEraData、字数统计）。集成测试覆盖：项目管理流程（创建项目→新增章节→写作→保存）。
- **优先级**：P1
- **预计工时**：6小时
- **验收标准**：
  - API 服务测试覆盖率 > 80%
  - Context Reducer 测试覆盖率 > 90%
  - 集成测试覆盖核心用户路径
  - `npm test` 全部通过

---

### 3.2 手动功能验收测试
- **任务名称**：手动功能验收测试
- **详细描述**：按照 SPEC.md 验收标准逐项手动测试：APK 安装、项目 CRUD、章节 CRUD、AI 润色、AI 历史还原、AI 诗词推荐、时代背景切换。记录所有发现的问题并创建 Issue。
- **优先级**：P0
- **预计工时**：4小时
- **验收标准**：
  - 所有 SPEC.md 验收标准均已验证
  - 无 P0 级功能缺陷遗留
  - 测试报告已生成

---

### 3.3 性能与稳定性优化
- **任务名称**：性能与稳定性优化
- **详细描述**：
  - 优化大文本（5万字以上）输入性能，防止白屏和卡顿
  - 优化 AI 响应展示，避免长文本导致 UI 卡顿
  - 增加错误边界（ErrorBoundary），防止单页崩溃导致整个应用崩溃
  - 优化 AsyncStorage 读写，避免频繁 IO 阻塞 UI
- **优先级**：P1
- **预计工时**：4小时
- **验收标准**：
  - 5万字文本输入无明显卡顿
  - AI 响应展示流畅
  - 应用全局有 ErrorBoundary 保护

---

### 3.4 APK 构建与签名
- **任务名称**：APK 构建与签名
- **详细描述**：
  - 使用 `npx expo run:android --variant release` 构建生产 APK
  - 配置 release.keystore 签名（提供默认签名文件或指导用户配置）
  - 验证 APK 大小合理（目标 < 50MB）
  - 在真机上安装测试，确认功能完整（特别是 AI 调用）
- **优先级**：P0
- **预计工时**：3小时
- **验收标准**：
  - APK 成功构建，无编译错误
  - APK 可在 Android 7.0+ 真机上正常安装
  - APK 内包含 JS Bundle，脱离开发服务器可独立运行
  - AI 功能在 APK 中正常调用

---

### 3.5 Release 发布
- **任务名称**：GitHub Release 发布
- **详细描述**：
  - 创建 `CHANGELOG.md` 记录本版本功能变更
  - 在 GitHub 创建 Release，附上 APK 文件
  - 更新 `README.md` 包含版本说明和快速开始指南
  - 同步更新 `SPEC.md` 验收标准（已完成项打勾）
- **优先级**：P0
- **预计工时**：2小时
- **验收标准**：
  - GitHub Release 已创建并包含 APK 附件
  - CHANGELOG.md 记录完整
  - README.md 包含版本说明和联系方式

---

### Phase 3 验收标准汇总
- [ ] 所有单元测试通过
- [ ] 手动验收测试无 P0 级缺陷
- [ ] 性能优化已实施，大文本场景无明显卡顿
- [ ] APK 构建成功，大小 < 50MB
- [ ] APK 可独立运行（无需 Metro 开发服务器）
- [ ] GitHub Release 已创建
- [ ] CHANGELOG.md 和 README.md 已更新

---

## 任务汇总

| Phase | 任务数 | 预计总工时 |
|-------|--------|-----------|
| Phase 1：项目初始化 | 7 | 18小时 |
| Phase 2：核心功能开发 | 12 | 42小时 |
| Phase 3：测试与发布 | 5 | 19小时 |
| **合计** | **24** | **79小时** |

---

## 优先级说明

- **P0**：阻塞性功能，必须在正式版发布前完成
- **P1**：核心功能，影响用户体验，但不阻塞发布
- **P2**：增强功能，可接受延期至后续版本

---

## 开发顺序建议

1. **Phase 1** 建议在第 1-2 周完成（并行推进）
2. **Phase 2** 建议在第 3-6 周完成（模块间无强依赖，可按团队情况调整）
3. **Phase 3** 建议在第 7-8 周完成（功能稳定后开始测试）
